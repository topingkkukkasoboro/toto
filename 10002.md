```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();

        if (a > b) {
            System.out.println(">");
        }
            else if (a < b)
                System.out.println("<");

        else if (a == b)
        System.out.println("==");
    }
    }
// if, else if는 조건문이다 if (a > b) if 뒤에 있는 (a>b)가 조건이다 (a가 b보다 크냐?) 이런식으로
// if는 말그대로 뭐뭐 한다면 if (a > b)는 a가 b보다 크다면으로 읽을 수 있다 // 괄호 안에 조건값이 일치 즉 참이라면
// 작성해둔 system.out.println으로 출력되게 된다

//else if는 또한 만약에로 앞에 작성한 if에서 작성한 조건값이 참이 아니라면 ex) if (a>b)인데 b가 값이 더 큰상황
//else if로 작성하여 한번 더 조건을 제시한다 (a < b) b가 a보다 크냐?로 // 괄호 안에 조건 값이 일치 즉 참이라면
//작성해둔 system.out.println으로 출력되게 된다
// 그런데 일치 하는 조건 값이 없다면 아무것도 출력되지 않는다 그럼으로 가능한 조건값을 넣어야 한다.
```
```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int score = sc.nextInt();

        if (90 <= score && score <= 100) {
            System.out.println("A");
        }
        else if (80 <= score && score <= 89) {
            System.out.println("B");
        }
        else if (70 <= score && score <= 79) {
            System.out.println("C");
        }
        else if (60 <= score && score <= 69) {
            System.out.println("D");
        }
        else if (0 <= score && score <= 68) {
            System.out.println("F");
        }
    }
    }
// 여기서는 score의 값을 어떻게 출력하냐로 작성한 것이다 ex) 시험점수로 등급 메기기 - 1등만 기억하는 더러운 세상
//숫자로 조건을 제시했고 범위를 정했다 (90 <= score && score <= 100)
// 자바는 귀찮게 설명을 많이 해줘야 한다. 해석하면 (90 < = score) 90보다 크거나 같으면 (&&) 그리고 (score <= 100)
//score가 100보다 작거나 같다면 // 조건이 2가지인 경우 &&를 통해 그리고라는 표현을 해줘야한다
```

```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int year = sc.nextInt();

// 윤년을 구하는 문제이다 조건이 까다로웠다 // 년수가 4의 배수이면서 100의 배수가 아닐때 혹은 400의 배수일때 윤년이다 윤년인지 확인 가능한 코드를 만들자
// 윤년이면 1 아니면 0이 나와야한다
        if (((year%4)==0 && (year%100)!=0) || (year%400)==0) {// 말을 이해 못해서 그대로 작성했다 // 나머지를 구하는 방식으로 참인지 거짓인지 구했다
// y를 %했을때 나머지가 0과 같으며 100을%했을때 0과 다르거나 400을 %했을때 나머지가 0과 같아야 한다
            System.out.println(1);
        } else{
            System.out.println(0);
        }

    }
}


```

``` java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
// 4분면을 구하는 문제이다 a와 b의 수가 주어지면 해당 수는 어느 면에 해당하는지 구해야 했다
// 말 그대로 작성하면 되는 문제 였다
// 수는 양수와 음수로만 주어지기에 0으로 비교를 하면된다
        if (a > 0 && b > 0 ){
            System.out.println(1);
// a가 0보다 크고 b가 0보다 크다면 1을 출력해라
        }
        if (a<0 && b> 0){
            System.out.println(2);
// a가 0보다 작고 b가 0보다 크다면 2를 출력해라
        }
        if (a < 0 && b < 0){
            System.out.println(3);
//a가 0보다 작고 b도 0보다 작다면 3을 출력해라
        }
        if (a > 0 && b < 0){
            System.out.println(4);
// a가 0보다 크고 b가 0보다 작으면 4를 출력해
        }
    }
}

```

```java
//오늘부터 나는 알람시계를 혐오할것이고 존나 싫어졌다
// 정석으로한 풀이부터 보겠다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int h = sc.nextInt();
        int m = sc.nextInt();

// 두 수가 주어진다 a는 시간 b는 분이다 상근이새끼가 45분 알람을 일찍설정할 수 있게 하는것이다
// 시간은 23을 넘을수가 없고 분은 59를 넘을 수가 없다

        if (m == 45){ // 현재 설정 되어 있는 분 즉 "m"이 45와 같다면 m을 0으로 초기화 해서 출력하면 된다 h는 그대로
            System.out.println(h);
            System.out.println(0); 
        }
        if ((m > 45)){ // 현재 설정 되어 있는 분 m이 45보다 크다면 m - 45를 하여 출력하면 된다 h는 그대로
            System.out.println(h);
            System.out.println(m-45);
        } else if (m < 45) { // 시발 1시간 땡겨서 일어날것이지 // 현재 설정되어 있는 분 m이 45보다 작다면 // h에서 1을뺀 값이 h가 되고 h가 0보다 작으면 h를 23으로 초기화 해라
            h = h-1;
            if(h<0){
                h = 23;
            }
            System.out.println(h);
            System.out.println(m + 60 - 45); // m에 60을 더한뒤 45를 뺀 값을 출력


        }


    }
}


```

```java
//방금과 같은 문제인데 난 문제를 봤을때 이런 풀이가 떠올랐다
//1시간은 60이니까 시간에 60을 곱해서 분으로 만들어서 현재 분과 더하고 거기에 45를 -해서 60만큼 나눈수가 시간이고 남은게 분아니야?라고
//if안에 if 넣을수 있다는걸 잊어버린거 같았다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int h = sc.nextInt();
        int m = sc.nextInt();
        int a = 0;

            h  = ((h * 60) + m)-45; // 시간을 60으로 곱한뒤에 분을 더하고 45를 빼자 그럼 그게 h값
            for (int i=0; i < h; h -=60){ // i라는 수를 하나 만들어서 시간을 측정한다 // h에서 60을 뺼수없을떄까지 빼고 뺀만큼의 횟수를 a에 더한다 // a는 시간 // 남은 h는 분
                a++;
            }
            if (a == 0){ // a가0과 같으면 a에 23을 더하고 h에 60을 더해라
                System.out.print(a+23 + "시");
                System.out.print(h+60 + "분" );
            }
            else {
                System.out.print(a - 1 + "시"); //그게 아니라면 a에 1을 빼고 // h에 60을 더해라
                System.out.print(h + 60 + "분" );
            }

        }

    }


```
