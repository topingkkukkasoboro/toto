```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();

        // 구구단 문제 // 수가 주어지면 1~9까지 곱한 값을 출력하자
        for (int a = 1; a <= 9 ; a++){ // a에 1을 대입, a가 9와 같아질때 까지, a에 1씩 더하자
            System.out.printf("%d * %d = %d%n", n, a, n * a);
        }
        // %d를 사용하니 풀렸다.... %d를 사용하면 해당 위치에 숫자가 들어간다...
        // %n을 하면 줄바꿈이 생긴다
        // printf로 적어야지 작동을 한다
    }
}

```

```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int T = scanner.nextInt();
// 문제를 이해 못했다 예제를 보고 T에 값을 넣으면 랜덤하게 더해야 하나 싶었다
// 내가 잘못 봤다

        for (int c = 0; c < T; c++) { // c에 0으로 초기화 해라 (그리고) c보다 T가 클때까지 (뭘?) c에다 1씩 플러스해라
            int A = scanner.nextInt(); // A의 값을 스캔
            int B = scanner.nextInt(); //B의 값을 스캔

            System.out.println(A + B); // A +B의 값 출력
        }
    }
}

```

```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt(); // a는 1보다 크고 10000보다 같거나 작다
        int b = 0;
        int c = 0;  /  for문 {}안에 선언을 하면 외부에서 안 뽑히더라 // 밖에서 선언을 하자

        for (; b <= a; b++){ // b가 a와 같아질때까지 (뭘?) b에 1씩 더해라
            c = b + c; // c에다가 b + c를 대입해라 (언제까지?) b가 a와 같아질떄까지
        }
        System.out.println(c); // c 뽑아라
        }


    }


```
```java
// 대문자 소문자를 구분해서 클라이언트의 요구대로 진행하자
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int x = sc.nextInt(); // 영수증의 총 금액
        int n = sc.nextInt(); // 물건의 종류 수
        int c = 0; // 저장소를 만듬 (a*b를할수있는)

        for (int i = 0; i < n; i++) { // i라는 값을 만들어서 n 즉 물건 종류수와 같아질때까지
            int a = sc.nextInt(); // 물건 가격
            int b = sc.nextInt(); // 물건 갯수
            c += a * b; // a*b의 값
// 이제 i가 n과 같아질때까지 a와 b를 해당 횟수만큼 수를 넣을 수 있다
        }
        if (x == c) {
            System.out.println("Yes");
        } else {
            System.out.println("No");
        }
    }

}


```
```java
// 4당 int가 한번씩 나오게 만들어 줘야한다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        String i = ""; // String에 "" 이렇게만 해도 된다 문자열이라는건 변하지 않는다

        for (int b = 0;b < a; a -= 4){
            i += "long "; // i가 String으로 문자열이니 문자를 더한다
        }
        System.out.println(i + "int");
    }

}

```
```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int t = sc.nextInt();
        int c = 0;

        for (int i = 1; i <= t; i++){
            int a = sc.nextInt();
            int b = sc.nextInt();

            c = a+b;
            System.out.println("Case #"+i+": "+c); // 출력마다 Case # 그리고 횟수별 숫자를 넣어야 한다
            // i에 횟수별 숫자가 더해지니 i를 더한다. Case #이나 :는 문자이니 ""을 더해서 출력하자
        }

    }

}

```
```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int t = sc.nextInt();
        int c = 0;

        for (int i = 1; i <= t; i++){
            int a = sc.nextInt();
            int b = sc.nextInt();

            c = a+b;
            System.out.println("Case #"+i+": "+ a + " + " + b + " = " + c);
//방금 문제보다 더 아름답게 해본다고 하였다?
//("Case #"+i+": "+ a + " + " + b + " = " + c) 이걸 해석하면 Case #1: 1 + 1 = 2 이런식으로 나온다
// :나 +에 띄워쓰기를 추가하여 출력 될때 띄워쓰기도 표현할 수 있다다
        }

    }

}

```
```java
//문제는 n의 숫자 만큼 *을 표현해내는 것이다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        String b = ""; // 문자를 표현해야하니까 String으로 적어 문자열을 입력할 수 있게 한다

        for (int a = 0; a < n; a++){ // a <= n으로 표현하니 *이 하나 더 나왔다, // 한번 더 표현됨으로 <로만 표현해준다
            b += "*";
            System.out.println(b);
        }

    }

}


```
```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt(); // n에 정수를 스캔
        String b = ""; // b를 문자열 초기화
        String c = ""; // c를 문자열 초기화

        for (int a = (n -1) ; a >= 0; a--){ // a는 n -1이다; a가 0보다 크거나 같을때까지; a에 1씩빼라
            b += "*"; // b에는 문자열 *을 넣어라 //작은 수부터 시작 그럼은로 한개씩 시작
            for (int d = 1; d <= a; d++) // d는 1; d보다 a가 크거나 같을때까지; d에 1씩 더해라
               c += " "; // d에는 문자열 띄워쓰기를 넣어라 // 큰 수부터 시작 그럼으로 4씩 시작작
            System.out.print(c);// c를 출력
            System.out.println(b); // d를 출력
            c = "";// 문자열 c를 초기화 해라
        }

    }

}
//  *
   **
  ***
 ****
*****가 나오게 출력하는데... 이건 좀 너무 했다
```
```java
// 여기서는 문제기가 수를 입력하는 것을 일렬로 넣어야 하고 //계속 반복되게 해야하고
// 0과 0을 더할 경우에는 종료되게 해야 한다.
// 문제를 해결하기 위해서는 일렬로 넣는방법은 먼저 해야했다
// String문으로 먼저 수를 넣게 해야 했다 // 일렬도 넣는 법은 스페이스바로 띄우면 2개로 인식되게
//숫자를 1 띄우고 2 이런식으로 넣어서 각각 1과 2가 구분되어야 했는데 방법은 간단했다
// 입력한것의 몇번째? 정하는 법은 .split(" ")[0]이다 // .split 앞에는 문자열을 넣는다
// 그 뒤 (" ")을 넣은뒤 []칸 안에 몇번쨰인지 수를 넣어준다 // 0부터 시작한다
// 그리고 Integer.parseInt를 이용해 String으로 넣은 문자열을 숫자로 변환해준다
// 그러면 몇번쨰수를 사용하는 방법이 생기니 이걸로 값을 구하면 된다
// 무한대로 조건을 걸때는 조건을 안걸면 된다 for(int i=0;;i++ ) 처럼
// i에는 0이라는걸 넣고 i를 어떻게 해라! 라는 말이 없고 i를 계속 1씩만 더하기에 할게 없으니 계속 반복된다
//for문을 이중이나 삼중으로 쓰는 방법도 알아야 한다.
// for(int i = 0; i < 5; i++ ){
//}
//for(int d = 0; d < 5; d++){
//}
// 해당문법을 보면 {}를 꼭 확인해야한다 //for(int i = 0; i < 5; i++ ){ } <- 안에 for(int d = 0; d < 5; d++)이 있음으로
// for(int d = 0; d < 5; d++)가 조건이 성립 될때까지 작동한 후에 for(int i = 0; i < 5; i++ )이 진행된다
// for(int d = 0; d < 5; d++)가 끝나면 d=6이 될건데 for문이 끝나면 초기화를 해서 다시 d = 0이 된다다
import java.util.Scanner;

public class Main {
	public static void main(String[] args) {
		Scanner sc = new Scanner (System.in);
		
		for(int i=0;;i++ ) { // 조건이 없기 떄문에 무한대로 작성됨..
			String temp = sc.nextLine(); // temp의 문자열을 스캔
			
			if(temp.equals("0 0")){ // 만약에 temp에 있는게 0 0과 같다면
				System.out.println(""); // 종료됩니다를 출력해라
				break; // 빠져 나와라
			}
			
			int num1 = Integer.parseInt(temp.split(" ")[0]);
			int num2 = Integer.parseInt(temp.split(" ")[1]);
			
			//System.out.println("num1 = " + num1);
			//System.out.println("num2 = " + num2);
			System.out.println(num1 + num2);
			//System.out.println(temp);

			
		}
	}

}


```
```java
// 여기서는 while을 사용한다
// while이란 말글대로 ~~하는동안이며  while뒤에 붙은()안에 조건이 참일동안 계속 돌아가는방식이다
//이것 또한 {}이것으로 시작 끝을 표시해준다
// 이번엔 has.Next.Int를 사용했는데 Has의 뜻은 가진다로 해석하면 내가 정수를 입력하는 동안으로 읽을수있다
//이럴 경우 while에 } 끝에서 sc.close();로 끝낼수있다 여기서 sc는 스캐너를지정한 이름으로 자유롭게 바꿀수있다다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

       while(sc.hasNextInt()){

           int a = sc.nextInt();
           int b = sc.nextInt();

           System.out.println(a+b);
       }
       sc.close();
       }
       }

```
