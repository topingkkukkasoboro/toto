```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();

        // 구구단 문제 // 수가 주어지면 1~9까지 곱한 값을 출력하자
        for (int a = 1; a <= 9 ; a++){ // a에 1을 대입, a가 9와 같아질때 까지, a에 1씩 더하자
            System.out.printf("%d * %d = %d%n", n, a, n * a);
        }
        // %d를 사용하니 풀렸다.... %d를 사용하면 해당 위치에 숫자가 들어간다...
        // %n을 하면 줄바꿈이 생긴다
        // printf로 적어야지 작동을 한다
    }
}

```

```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int T = scanner.nextInt();
// 문제를 이해 못했다 예제를 보고 T에 값을 넣으면 랜덤하게 더해야 하나 싶었다
// 내가 잘못 봤다

        for (int c = 0; c < T; c++) { // c에 0으로 초기화 해라 (그리고) c보다 T가 클때까지 (뭘?) c에다 1씩 플러스해라
            int A = scanner.nextInt(); // A의 값을 스캔
            int B = scanner.nextInt(); //B의 값을 스캔

            System.out.println(A + B); // A +B의 값 출력
        }
    }
}

```

```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt(); // a는 1보다 크고 10000보다 같거나 작다
        int b = 0;
        int c = 0;  /  for문 {}안에 선언을 하면 외부에서 안 뽑히더라 // 밖에서 선언을 하자

        for (; b <= a; b++){ // b가 a와 같아질때까지 (뭘?) b에 1씩 더해라
            c = b + c; // c에다가 b + c를 대입해라 (언제까지?) b가 a와 같아질떄까지
        }
        System.out.println(c); // c 뽑아라
        }


    }


```
```java
// 대문자 소문자를 구분해서 클라이언트의 요구대로 진행하자
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int x = sc.nextInt(); // 영수증의 총 금액
        int n = sc.nextInt(); // 물건의 종류 수
        int c = 0; // 저장소를 만듬 (a*b를할수있는)

        for (int i = 0; i < n; i++) { // i라는 값을 만들어서 n 즉 물건 종류수와 같아질때까지
            int a = sc.nextInt(); // 물건 가격
            int b = sc.nextInt(); // 물건 갯수
            c += a * b; // a*b의 값
// 이제 i가 n과 같아질때까지 a와 b를 해당 횟수만큼 수를 넣을 수 있다
        }
        if (x == c) {
            System.out.println("Yes");
        } else {
            System.out.println("No");
        }
    }

}


```
```java
// 4당 int가 한번씩 나오게 만들어 줘야한다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        String i = ""; // String에 "" 이렇게만 해도 된다 문자열이라는건 변하지 않는다

        for (int b = 0;b < a; a -= 4){
            i += "long "; // i가 String으로 문자열이니 문자를 더한다
        }
        System.out.println(i + "int");
    }

}

```
```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int t = sc.nextInt();
        int c = 0;

        for (int i = 1; i <= t; i++){
            int a = sc.nextInt();
            int b = sc.nextInt();

            c = a+b;
            System.out.println("Case #"+i+": "+c); // 출력마다 Case # 그리고 횟수별 숫자를 넣어야 한다
            // i에 횟수별 숫자가 더해지니 i를 더한다. Case #이나 :는 문자이니 ""을 더해서 출력하자
        }

    }

}

```
```java

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int t = sc.nextInt();
        int c = 0;

        for (int i = 1; i <= t; i++){
            int a = sc.nextInt();
            int b = sc.nextInt();

            c = a+b;
            System.out.println("Case #"+i+": "+ a + " + " + b + " = " + c);
//방금 문제보다 더 아름답게 해본다고 하였다?
//("Case #"+i+": "+ a + " + " + b + " = " + c) 이걸 해석하면 Case #1: 1 + 1 = 2 이런식으로 나온다
// :나 +에 띄워쓰기를 추가하여 출력 될때 띄워쓰기도 표현할 수 있다다
        }

    }

}

```
```java
//문제는 n의 숫자 만큼 *을 표현해내는 것이다
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        String b = ""; // 문자를 표현해야하니까 String으로 적어 문자열을 입력할 수 있게 한다

        for (int a = 0; a < n; a++){ // a <= n으로 표현하니 *이 하나 더 나왔다, // 한번 더 표현됨으로 <로만 표현해준다
            b += "*";
            System.out.println(b);
        }

    }

}


```
